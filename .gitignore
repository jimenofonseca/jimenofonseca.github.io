# Not sure what a .gitignore is?
# See: https://git-scm.com/docs/gitignore

# These are directly copied from Jekyll's first-party docs on `.gitignore` files:
# https://jekyllrb.com/tutorials/using-jekyll-with-bundler/#commit-to-source-control

# Ignore the default location of the built site, and caches and metadata generated by Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Ignore folders generated by Bundler
.bundle/
vendor/

# Project-local launch config (per-machine, not for deployment)
.claude/

# Node + private content
node_modules/
private-src/
.staticrypt.json

# Photography originals (kept on local machine only — too big for git)
assets/photography/_originals/

# Music originals (raw clips before compression — too big for git)
assets/music/_originals/

# Source-of-truth video clip (replaced after ffmpeg pass with music.mp4)
assets/music/My Movie.mp4

# macOS metadata — never useful in repo
.DS_Store
**/.DS_Store
